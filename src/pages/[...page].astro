---
// 此文件已弃用，博客内容已迁移到 /blog/[...page].astro
// 首页内容见 /index.astro
import { redirect } from 'astro';

export const prerender = true;

// 重定向所有流量到新路由
export const getStaticPaths = async () => {
    return [
        { params: { page: undefined }, props: {} }
    ];
};

return redirect('/');
---